# 剧情英语听写网站：开发提示词与实施计划

> 本版更新（2026-08-23）：新增「本地视频文件模式」——网站不托管任何剧集视频，用户从自己电脑选取合法拥有的视频在页面内播放，文件不上传、不入仓库。改动集中在第四节、第九节、第十节验收标准与实施计划对应行，其余内容与原版一致。

## 一、产品目标

这个网站解决的问题是：英语学习者看得懂剧情大意，却无法准确拼出台词，也缺少从听写到开口跟读的连续训练。

核心学习闭环：

> 看剧情画面 → 理解语境 → 输入英文台词 → 获得逐字反馈 → 答对后听原句 → 跟读录音 → 查看每日学习统计

主要使用场景是用户坐在电脑前，一边看视频或场景画面，一边使用键盘完成听写。因此产品必须采用桌面网站形态，而不是移动端 App 式的纵向卡片界面。

---

## 二、可直接复制使用的开发提示词

```text
你是一名同时具备产品思维、UI/UX 设计能力和前端工程能力的 Product Engineer。请为我设计并实现一个“跟着剧情学英语”的桌面网站。

一、用户问题

用户在观看英文剧情时，常常只能被动阅读字幕，无法确认自己能否准确听懂、拼写和说出台词。网站需要把被动观看转化为“理解语境、主动听写、听音确认、开口跟读”的学习闭环。

二、核心设备与页面形态

1. 这是 desktop-first 的网站，不是移动端 App。
2. 优先适配 1440×900 和 1280×800 的电脑屏幕。
3. 桌面布局采用两列：
   - 左侧：当前场景标题、剧情简介、人物、语气、剧情提示、每日学习统计。
   - 右侧：上方为 16:9 视频或场景画面，下方紧接字幕听写模块。
4. 用户在滚动较少的情况下，应能清楚完成“看画面 → 输入台词”。
5. 移动端只保留基本兼容，不要以手机布局反向决定桌面结构。

三、视觉方向

采用高级、克制的 Swiss editorial 风格：

- 背景使用近黑色：面板 #17171B，版面底 #0B0B0E，画面区 #08080A。
  暗底是为了让视频不再是白底上的一块亮斑，眼睛不用在明暗之间反复适应。
- 字体使用 Helvetica Neue、Helvetica 或 Arial。
- 主文字使用接近白色 #E9E9EC，次要文字 #92929C；唯一强调色使用提亮版 Swiss Red #FF3355
  （印刷用的 #E4002B 在暗底上发闷）。
- 使用清晰网格、1px 分隔线、大幅剧情画面和充分留白。
- 不使用暖色纸张纹理、渐变、玻璃拟态、圆角卡片、柔和阴影或荧光撞色。
- 画面是视觉中心，界面装饰不能抢夺学习注意力。

四、剧情画面（本版重点修订）

网站本身不托管、不分发任何剧集视频。画面来源分两种模式：

1. 本地文件模式（个人使用的主路径）：
   - 练习在 content.js 中声明 mediaMode: "local"，表示该课程的视频由用户自行提供。
   - 页面显示 16:9 占位框和「选择本地视频」按钮（<input type="file" accept="video/*,audio/*">），
     用户从自己电脑选取合法拥有的视频或音频文件。
   - 选中后用 URL.createObjectURL 在页面内播放，文件不上传、不写入仓库、不存入 localStorage。
   - 每条练习可配置 startTime 和 endTime（单位：秒），本地长视频加载后自动跳到 startTime，
     播放到 endTime 自动暂停，并提供“重播本句”按钮循环该片段。
   - 刷新页面后浏览器安全机制要求重新选择文件，占位框需给出友好提示，而不是报错。
   - 未选择文件时，听写模块必须仍可独立使用（听写不依赖视频加载成功）。
2. 内置素材模式：
   - 每条练习支持 imageSrc、imageAlt、videoSrc 和 audioSrc。
   - 有 videoSrc 时播放视频，并使用 imageSrc 作为 poster；没有视频时显示场景图片。
   - 仓库内置素材只能是原创内容、模拟内容或公有领域素材（如 Internet Archive 上
     版权已过期的影视），必须在 README 中标注每个素材的来源和许可。

通用规则：

3. 画面不显示英文字幕，避免用户直接抄答案。
4. 公开仓库不得包含未授权的电视剧视频、字幕、原声或演员图片。
5. 用户的本地文件只存在于其电脑和当前浏览器会话中，与仓库、与网站部署方无关。

五、字幕听写

1. 听写模块必须放在视频画面正下方，不能独立放在页面右侧。
2. 显示中文意思，但默认隐藏完整英文答案。
3. 使用逐字轨道显示尚未输入、正确和错误位置。
4. 最终判定忽略大小写和标点差异。
5. 用户可以主动显示或隐藏提示。
6. 空答案不能提交，并给出明确提示。
7. 错误答案不能解锁完整原句声音或跟读功能。
8. 答对后显示完整英文台词，并解锁“播放原句”和“开始跟读”。

六、输入互动与声音

1. 正确字母播放轻快、短促的键音。
2. 错误字母播放较低的提示音，错误位置标红，输入框轻微水平震动。
3. 每正确完成一个单词，使用浏览器英文语音朗读该单词。
4. 实时显示简短反馈，例如：
   - “对了，保持这个节奏。”
   - “这个字母不太对，看看红色的位置。”
   - “need 完成，听听它的发音。”
5. 必须提供“声音：开/关”，并保存用户选择。
6. 删除字符不能误触发正确音或单词朗读。
7. 声音反馈使用 Web Audio API 和 SpeechSynthesis，避免引入额外音效依赖。

七、跟读练习

1. 答对后才能解锁。
2. 支持播放原句；本地文件模式下“播放原句”即重播当前片段（startTime 到 endTime），
   没有视频且没有 audioSrc 时使用浏览器英文语音作为替代。
3. 支持麦克风录音、停止录音和本地回放。
4. 麦克风权限被拒绝时要给出可理解的提示，页面不能报错。
5. 录音只保存在当前页面，不自动上传。

八、每日学习统计

按用户电脑的本地日期自动分天记录：

- 每天完成的句子数，同一句每天最多计入一次。
- 正确输入完成的单词数。
- 检查答案次数。
- 播放原句次数。
- 跟读录音次数。
- 有效学习时长。
- 连续学习天数。
- 最近 7 天的每日有效操作柱状图。

有效学习时长口径：页面处于可见状态，并且用户最近 90 秒内有键盘或鼠标操作时，每 15 秒累计一次。不能把单纯打开网页当成持续学习。

第一版使用 localStorage 保存，最多保留最近 90 天。刷新后数据不能丢失。换浏览器、清理浏览器数据或换设备后不能同步，这个边界必须写进 README。

九、技术约束（本版重点修订）

1. MVP 使用原生 HTML、CSS、JavaScript，不增加第三方依赖。
2. 可以直接部署到 GitHub Pages。
3. 内容数据与页面逻辑分离，练习内容统一放在 content.js。
4. 本地视频文件的处理规则：
   - 用 URL.createObjectURL 生成播放地址，切换练习或页面卸载时用 revokeObjectURL 释放，
     避免内存泄漏。
   - 禁止尝试把视频内容写入 localStorage（容量超限且无意义）；最多记录“上次选择的
     文件名”，用于刷新后提示用户重新选择同一个文件。
   - 刷新后必须重新选择文件是浏览器安全边界，不是 bug，要写进 README 的已知边界。
5. 使用语义化 HTML，按钮可用键盘操作，动态提示使用 aria-live。
6. 支持 prefers-reduced-motion。
7. 不执行 build 命令。
8. 代码注释使用中文，只解释非显而易见的逻辑。

十、验收标准

请使用真实浏览器自动化完成以下验证：

1. 1440px 和 1280px 下保持桌面网站结构。
2. 左侧为剧情和统计，右侧视频在上、听写在下。
3. 图片、视频或音频资源不存在加载失败。
4. 正确字母、错误字母和单词完成触发不同反馈。
5. 关闭声音后，键音和单词朗读都停止。
6. 错误答案不能解锁原句和录音。
7. 正确答案可以解锁原句和录音。
8. 每日统计会随输入、检查、播放和录音实时更新。
9. 模拟昨天数据后，连续学习天数和最近 7 天图表计算正确。
10. 刷新页面后，完成进度、声音设置和每日统计仍然存在。
11. 本地文件模式：选择视频后能播放并自动跳到 startTime；到 endTime 自动暂停；
    刷新后显示“请重新选择文件”的占位提示而不是报错。
12. 本地文件模式下不选择任何文件，听写、判定、统计功能全部正常可用。
13. Console 没有未处理 error，Network 没有资源请求失败，
    也没有任何把本地文件内容发往外部的请求。

十一、本期明确不做

- 不做用户账号。
- 不做跨设备同步。
- 不做排行榜或社交社区。
- 不做 AI 发音评分。
- 不做整集电视剧在线播放和任何形式的视频托管。
- 不抓取、下载、搬运或公开托管未授权影视资源，也不内置任何指向盗版资源的链接。
- 不做视频文件的持久化存储（IndexedDB 缓存视频属于下期再议）。
- 不建立后端、数据库或管理后台。

完成后请输出：修改文件列表、功能说明、测试结果、已知边界、桌面端预览图，以及下一阶段建议。不要只提供代码片段，要交付可以直接运行的网站。
```

---

## 三、实施计划

### 阶段 0：确认输入条件

| 优先级 | 工作 | 负责人 / 系统 | 交付结果 |
|---|---|---|---|
| P0 | 确认网站主要设备为桌面电脑 | Dora | 桌面优先原则固定 |
| P0 | 确认公开仓库只使用合法素材 | Dora | 素材来源清单（原创 / 公有领域） |
| P0 | 确认仓库名称和公开/私有属性 | Dora | GitHub 仓库配置 |
| P1 | 挑选 1 部公有领域影片做内置示例课程 | Dora | `[待确认]` 示例影片（候选：Internet Archive 公版影视） |

> 变更说明：原「确认是否拥有可公开使用的视频和原声」一项已由本地文件模式化解——正式学习内容走本地模式，无需公开授权；仓库只需要示例素材。

### 阶段 1：完成可上线 MVP

| 优先级 | 工作 | 负责人 / 系统 | 验收标准 |
|---|---|---|---|
| P0 | 桌面两列网站布局 | 前端 | 左侧语境，右侧视频与听写 |
| P0 | 本地视频文件模式 | 前端 | 选取→播放→片段跳转→刷新后友好提示 |
| P0 | 内置素材切换（图片/视频） | 前端 + `content.js` | 每条练习正确显示对应画面 |
| P0 | 逐字输入与答案判定 | 前端 | 正误反馈准确，忽略大小写和标点 |
| P0 | 输入键音与单词朗读 | 浏览器音频能力 | 正确、错误、单词完成反馈可区分 |
| P0 | 答对后播放和录音 | 浏览器媒体能力 | 错误时锁定，正确后解锁 |
| P0 | 每日学习统计 | `localStorage` | 分天记录、刷新保留、7 天图表正确 |
| P0 | 桌面端自动化验收 | QA / 浏览器测试 | 核心学习路径全部通过 |
| P0 | GitHub Pages 发布 | GitHub | 获得可访问网址 |

### 阶段 2：内容验证

| 优先级 | 工作 | 负责人 / 系统 | 验收标准 |
|---|---|---|---|
| P0 | 编写首批个人学习内容（台词文本 + 时间点） | Dora | 至少一个可完整学习的短场景集合 |
| P0 | 观察真实学习过程 | Dora | 记录卡住的位置和不必要的提示 |
| P1 | 调整提示强度与朗读速度 | 前端 | 声音不会打断连续输入 |
| P1 | 优化场景切分与 startTime/endTime 标注 | Dora | 每句有足够语境但不泄露答案 |

### 阶段 3：数据闭环

上线后关注以下指标，成功阈值暂不虚构：

| 指标 | 说明 | 成功阈值 |
|---|---|---|
| 开始输入率 | 看到画面后开始输入的人数占比 | `[待确认]` |
| 句子完成率 | 开始输入后最终答对的比例 | `[待确认]` |
| 平均完成时间 | 从第一次输入到答对所需时间 | `[待确认]` |
| 原句播放率 | 答对后点击播放原句的比例 | `[待确认]` |
| 跟读录音率 | 答对后完成一次录音的比例 | `[待确认]` |
| 声音关闭率 | 用户主动关闭互动声音的比例 | `[待确认]` |
| 连续学习天数 | 用户是否形成重复使用习惯 | `[待确认]` |

第一版没有后端分析系统，因此这些数据先用于个人本地复盘。若后续开放给其他用户，再选择隐私友好的数据分析方案。

### 阶段 4：根据真实需求扩展

只有 MVP 数据证明存在需求后再考虑：

| 优先级 | 候选功能 | 负责人 / 系统 | 启动条件 |
|---|---|---|---|
| P1 | 学习记录导出/导入 | 前端 | 用户需要备份或迁移设备 |
| P1 | 播放速度和循环次数 | 前端 | 用户反馈原句速度影响训练 |
| P1 | 自定义课程内容导入 | 前端 | 用户需要持续增加个人素材 |
| P1 | IndexedDB 缓存本地视频（刷新免重选） | 前端 | 刷新重选被证实是高频痛点 |
| P2 | 用户账号和云端同步 | 后端 + 数据库 | 明确存在跨设备使用需求 |
| P2 | AI 发音评分 | AI 服务 + 前端 | 跟读录音率证明用户愿意开口 |
| P2 | 内容管理后台 | 后端 + 管理前端 | 内容维护规模超过手工配置能力 |

---

## 四、本期范围总结

### 最小可上线版本

一个无需登录、可以直接通过 GitHub Pages 访问的桌面英语学习网站，包含：本地视频文件模式（用户自备合法素材）、合法内置示例画面、字幕听写、输入声音反馈、答对后听音和录音、浏览器本地每日统计。

### 本期明确不包含

账号、后端、数据库、跨设备同步、AI 发音评分、排行榜、内容后台、视频托管、视频持久化缓存，以及任何未授权影视资源的获取、托管或链接。

### 待确认项

- `[待确认]` GitHub 用户名、仓库名称及公开/私有属性。
- `[待确认]` 内置示例课程使用哪部公有领域影片（候选来源：Internet Archive）。
- `[待确认]` 每日学习成功目标，例如每天完成多少句或学习多少分钟。
- 网站定位：仅供个人使用（已确认，2026-08-23）。
